{
  "name": "homeledger-monorepo",
  "version": "1.0.0",
  "description": "HomeLedger - Personal and Household Expense Tracker (MERN Stack)",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"cyan,magenta\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "seed": "npm run seed --prefix server",
    "install:server": "npm install --prefix server",
    "install:client": "npm install --prefix client",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "build:client": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "expense-tracker",
    "finance",
    "mern",
    "react",
    "nodejs",
    "mongodb",
    "express"
  ],
  "author": "HomeLedger Team",
  "license": "MIT"
}
